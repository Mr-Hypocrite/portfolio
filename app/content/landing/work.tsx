import { Section, Text } from '@app/components';
import { FC } from 'react';

export const Work: FC = () => {
    return (
        <Section>
            <Text>work</Text>
        </Section>
    );
};
