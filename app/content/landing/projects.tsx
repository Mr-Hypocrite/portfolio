import { Section, Text } from '@app/components';
import { FC } from 'react';

export const Projects: FC = () => {
    return (
        <Section>
            <Text>projects</Text>
        </Section>
    );
};
