import { Section, Text } from '@app/components';
import { FC } from 'react';

export const Intro: FC = () => {
    return (
        <Section>
            <Text>intro</Text>
        </Section>
    );
};
