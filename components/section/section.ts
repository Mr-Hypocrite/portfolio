import { styled } from 'styled-system/jsx';

export const Section = styled('section', {
    base: {
        display: 'flex',
        minH: 'screen',
        w: 'full',
        alignItems: 'center',
        justifyContent: 'center'
    }
});
